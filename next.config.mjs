/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', 'localhost:8080'], // Allow images from localhost and localhost:8080
    },
  };
  
  export default nextConfig;